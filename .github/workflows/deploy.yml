name: CI/CD via GHCR to EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      GHCR_USER: ${{ secrets.GHCR_USER }}     # PAT owner
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}   # read:packages 권한 필수
    steps:
      - uses: actions/checkout@v4

      # Backend build
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Build backend (Gradle)
        run: ./gradlew clean build -x test

      # Push image to GHCR (빌드/푸시 후 IMAGE를 GITHUB_ENV로 노출)
      - name: GHCR login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push backend image
        run: |
          IMAGE="ghcr.io/${{ github.repository_owner }}/bus-app:${{ github.sha }}"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      # (선택) 키 파일로 쓰고 싶으면 사용, 아니면 바로 'key:'에 시크릿 넣어도 됨
      - name: Write SSH key
        run: |
          echo "${{ secrets.EC2_KEY }}" > key.pem
          chmod 600 key.pem

      # EC2 배포
      - name: Deploy backend on EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key_path: key.pem
          port: 22
          timeout: 1m
          command_timeout: 15m
          script_stop: true
          debug: true
          # 원격에 전달할 환경변수
          envs: IMAGE,GHCR_USER,GHCR_TOKEN
          script: |
            echo "IMAGE=$IMAGE"
            bash ~/deploy_docker.sh "${IMAGE}"